#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - print all the number of base i6 in lowercaase
*
* Return: 0
*/
int main(void)
{
int d;
char ch;

for (d = '0'; d <= '9'; d++)
{
	putchar(d);
}
for (ch = 'a'; ch <= 'f'; low++)
{
	putchar(ch);
}
putchar('\n');
return (0);
}
