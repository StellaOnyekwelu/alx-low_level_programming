#include "main.h"
/**
* _isupper - function that checks for uppercase character.
* @c: int type number
* Return: 1 if c is uppercase or 0
*/

int _isupper(int c)
{
if (c >= 0 && c <= 9)
return (1);
else
return (0);
}
